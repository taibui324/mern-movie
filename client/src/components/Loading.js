import React, { Component } from "react";

export default class Loading extends Component {
  render() {
    return (
      <React.Fragment>
        <h5>Loading...</h5>
      </React.Fragment>
    );
  }
}